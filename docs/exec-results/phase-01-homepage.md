# Phase 01 结果：前端团队总枢纽首页

## 1. 结果状态

Phase 01 第一版本地结果已完成。

本阶段目标是建立第一套技术基线、结构基线和设计基线，并在此基础上完成“前端团队总枢纽首页”的本地实现。

计划文件：

- `docs/exec-plans/phase-01-homepage.md`

本结果文件记录实际完成情况、主要文件、验证结果、已知问题和后续建议。

## 2. 本阶段实际完成的内容

已完成：

- 建立 Astro + React + TypeScript 技术基线。
- 使用 `pnpm` 管理依赖，并生成 `pnpm-lock.yaml`。
- 实现前端团队总枢纽首页。
- 将首页内容拆为 TypeScript 配置。
- 建立总枢纽首页的页面结构和基础样式。
- 建立可复用的区块标题组件。
- 更新结构基线、设计基线和正式决策记录。
- 完成本地构建检查。
- 完成本地开发服务 HTTP 访问检查。

明确未做：

- 未建设数据库。
- 未建设后台管理系统。
- 未建设登录和权限系统。
- 未接入真实业务数据。
- 未生成周报、月报或 PDF。
- 未配置生产部署。
- 未提交或推送 GitHub。

## 3. 创建和修改的主要文件

### 新增技术文件

- `package.json`：项目脚本和依赖声明。
- `pnpm-lock.yaml`：依赖锁定文件。
- `astro.config.mjs`：Astro 配置。
- `tsconfig.json`：TypeScript 配置。
- `src/env.d.ts`：Astro 类型声明入口。

### 新增首页实现文件

- `src/data/home.ts`：首页内容配置。以后调整首页文字、系统入口、入口状态和更新时间，优先看这个文件。
- `src/pages/index.astro`：首页页面结构和样式。
- `src/components/SectionHeading.astro`：复用的区块标题组件。

### 新增结果文件

- `docs/exec-results/phase-01-homepage.md`：Phase 01 首页执行结果。

### 修改的项目文档

- `README.md`：调整为项目级入口，增加 Phase 计划和结果索引。
- `docs/DECISIONS.md`：记录 Phase 01 技术基线、首页内容来源和总枢纽边界。
- `docs/STRUCTURE.md`：补充 Phase 01 首页结构基线和系统入口边界。
- `docs/DESIGN.md`：补充 Phase 01 设计基线，明确 `Hero_Ref.md` 不作为样式参考。

### 修改的项目配置

- `.gitignore`：忽略本地生成目录和 Windows 元数据文件。

## 4. 技术实现结果

当前技术栈：

- Astro 4
- React 18
- TypeScript
- pnpm

实现方式：

- 首页是 Astro 静态页面。
- React Islands 能力已通过集成保留，但当前首页没有强制加入复杂交互。
- 首页内容集中维护在 `src/data/home.ts`。
- `docs/references/Hero_Ref.md` 只作为首页应包含内容的范围来源。
- 页面没有直接渲染 `Hero_Ref.md`，也没有复刻其 Markdown 格式。

版本说明：

- 当前本地 Node 版本为 `18.19.1`。
- Astro 5 对 Node 版本要求更高，因此当前固定使用 Astro 4 + React 18。
- 未来升级 Node 后，可再评估是否升级 Astro 主版本。

## 5. 本地启动和访问方式

安装依赖：

```bash
pnpm install
```

启动开发服务器：

```bash
pnpm dev
```

打开终端输出的本地地址。

上一次运行地址为：

```text
http://127.0.0.1:4322/
```

如果端口被占用，Astro 会自动切换到下一个可用端口，应以终端输出为准。

## 6. 构建、检查和测试结果

已执行：

```bash
pnpm build
```

结果：

- Astro 类型检查通过。
- 静态构建通过。
- 首页 HTML 成功生成。
- 本地开发服务 HTTP 访问返回 `200 OK`。

已确认内容：

- 首页标题可渲染。
- 业务系统、组织与人员、绩效与报酬三个入口可渲染。
- 首页没有接入实时业务数据。
- 首页没有实现数据库、后台、登录或权限系统。

尚未执行：

- 浏览器截图检查。
- 多设备真实机检查。
- 可访问性自动化检查。
- 视觉回归检查。

## 7. 已知问题

- 当前首页只是本地实现，尚未部署到预览环境。
- 当前系统入口都是建设中或授权入口状态，尚未接入真实链接。
- 首页视觉只完成第一版网页化表达，仍需要人工审核信息层级、中文阅读节奏和移动端体验。
- 当前未配置格式化工具、lint 工具或测试框架。
- `docs/references/HERO_REF.md:Zone.Identifier` 是 Windows 元数据文件，已通过 `.gitignore` 忽略，不应作为项目资料使用。

## 8. 尚未完成的事项

- 确认三个系统入口的真实去向。
- 决定是否配置预览环境。
- 决定是否提交并推送 GitHub。
- 审核首页在桌面端和移动端的实际观感。
- 补充或调整首页内容配置。
- 根据审核结果决定是否进入 Phase 01 polish 或 Phase 02。

## 9. 后续建议

建议下一步按以下顺序处理：

1. 本地打开首页，审核内容结构和阅读体验。
2. 确认三个系统入口是继续保持建设中，还是接入外部智能文档。
3. 如需要，先做一轮首页信息层级和视觉微调。
4. 决定是否配置预览环境，方便非本机查看。
5. 审核无误后提交并推送 GitHub。
6. 再讨论 Phase 02 的业务系统入口或占位模块。

## 10. 最终内容与结构修正记录（2026/06/16）

本次修正范围：

- 只调整 Phase 01 首页内容结构和直接相关文档。
- 未修改 `AGENTS.md`。
- 未提交或推送 Git。

实际修改内容：

- 将首页用户转化路径调整为“看见 → 了解 → 选择 → 转换”。该表达后续在第 11 节修正为精简总结路径，不作为首页核心主旨或目标卡片标题。
- 将“我们为什么存在”区块改为轻量使命说明、目标卡片和前端边界提示。
- 删除原大型使命卡片。
- 保持“前端如何运作”以阶段任务为主标题、岗位为辅助信息。
- 将岗位摘要中的销售标记为“销售协作｜跨部门”。
- 删除首页“使用路径 / 不同用户接下来去哪里”完整区块。
- 删除首页“维护边界 / 本页只在结构性内容变化时更新”完整区块。
- 删除系统入口区块中的内部说明文案，使系统入口成为首页最后一个主要内容区块。
- 移除会进入构建产物的模板 HTML 注释，避免页面源码残留内部说明。

涉及文件：

- `src/data/home.ts`
- `src/pages/index.astro`
- `src/components/SectionHeading.astro`
- `docs/references/Hero_Ref.md`
- `docs/DESIGN.md`
- `docs/STRUCTURE.md`
- `docs/exec-plans/phase-01-homepage.md`
- `docs/exec-results/phase-01-homepage.md`

验证结果：

- `pnpm check`：通过，0 errors / 0 warnings / 0 hints。
- `pnpm build`：通过，成功生成 `dist/index.html`。
- 格式化：项目当前未配置 `format` 脚本，未执行独立格式化命令。
- lint：项目当前未配置 `lint` 脚本，未执行独立 lint 命令。
- 构建产物检查：未发现“使用路径”“维护边界”“总枢纽只说明结构”“周度变化”“项目数据和具体明细”等旧页面说明。
- 构建产物检查：当次检查中四阶段“看见、了解、选择、转换”已出现；后续第 11 节已修正其信息层级。
- 构建产物检查：系统入口区块之后页面直接结束，没有后续空白说明区块。
- 响应式检查：源码保留 `980px` 与 `640px` 两个断点，桌面端多列布局在平板和移动端逐步转为单列。

仍存在的问题：

- 项目尚未配置格式化、lint、浏览器截图、真实移动端或视觉回归检查工具。
- 本次未启动浏览器进行人工截图验收，桌面端和移动端最终观感仍需用户本地打开页面确认。

## 11. 内容层级修正记录（2026/06/16）

本次修正范围：

- 只修正首页“四阶段路径”和“我们为什么存在”区块的内容关系。
- 未修改 `AGENTS.md`。
- 未提交或推送 Git。

实际修改内容：

- 恢复首页核心主旨为三句：“让用户愿意点击我们的内容”“愿意读完、享受我们的内容”“愿意因此产生进一步咨询的兴趣”。
- 保留“看见 → 了解 → 选择 → 转换”作为 Hero 右侧的精简总结路径，不再作为首页核心主旨。
- 恢复“我们为什么存在”区块四个详细目标：“获得注意力、完成阅读、建立兴趣、形成潜客”。
- 将“我们的使命：”和使命说明合并到同一个紧凑文本区域中，避免拆成距离很远的两块。
- 同步 `Hero_Ref.md`、`DESIGN.md` 和 `STRUCTURE.md`，明确核心主旨、精简总结路径和详细目标是三个不同层级，不应互相替换。

涉及文件：

- `src/data/home.ts`
- `src/pages/index.astro`
- `docs/references/Hero_Ref.md`
- `docs/DESIGN.md`
- `docs/STRUCTURE.md`
- `docs/exec-results/phase-01-homepage.md`

## 12. Reference 使用状态记录（2026/06/16）

本次修正范围：

- 只整理 Reference 使用规则和阶段结果记录。
- 未修改页面代码。
- 未提交或推送 Git。

实际修改内容：

- `docs/references/Hero_Ref.md` 已完成使用并标记为 `Consumed / 已使用`。
- 后续首页修改不再默认依赖 `docs/references/Hero_Ref.md`。
- Reference 生命周期与使用规则已加入 `AGENTS.md`。
- 后续首页修改应优先依据当前产品文档、设计文档、结构文档、正式决策和实际实现。
