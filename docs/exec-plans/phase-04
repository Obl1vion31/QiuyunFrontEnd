下面这版可以直接给 Codex。它会说明清楚业务目标和已确认规则，但不会把数据库表、页面结构、字段名称和实现方式规定得过死。

````text
请结合当前仓库的实际代码和数据库结构，为“总周期规划表”制定一份开发 Plan。

本轮先不要修改代码，也不要创建数据库迁移。请先阅读项目，再根据现有实现提出方案。

这份 Plan 的重点是帮助我们确认：

1. 总周期规划表应该如何融入现有系统；
2. 哪些现有数据和组件可以直接复用；
3. 需要新增哪些最小必要的数据能力；
4. 页面和交互应该如何设计；
5. 后续应当如何分阶段开发。

请不要为了满足需求而重新设计整套内容系统，也不要拘泥于某一种预设实现。你可以根据当前仓库的真实情况，自行选择更合适的数据模型和界面方案，并说明理由。

# 一、当前项目背景

当前项目使用：

- Astro 4
- React 18
- TypeScript
- pnpm
- Neon PostgreSQL
- Drizzle ORM

当前已经有一个可真实读写数据库的“内容发布控制台”，主要完成具体内容和发布排期管理。

现有核心数据结构包括：

```text
operations_subject
  学科字典

operations_category
  帖子分类字典

operations_subject_category
  学科与可用帖子分类之间的关系

operations_content
  稳定的帖子身份

operations_content_version
  内容版本，例如 V0、V1、V2

operations_content_schedule
  具体内容版本的发布与推广排期
````

现有系统的核心关系是：

```text
帖子身份
→ 内容版本
→ 发布排期
```

学科和帖子分类之间已经存在可用关系，数据库、前端和服务端也已有相应校验。

目前已有：

* TMUA
* STEP
* 面试课

以及一组通用帖子分类。

请以当前仓库中的真实表、字段、接口、组件和页面为准，不要仅根据上面的描述猜测实现。

# 二、本阶段唯一目标

本阶段只推进“总周期规划表”。

它位于具体内容排期的上游，用于表达：

* 某一个学科；
* 某一种帖子分类；
* 在一年 1 月到 12 月之间；
* 哪些月份需要进行规划；
* 每个月大致采用什么发布安排；
* 对应的规则文档或内容文档在哪里。

它主要是一个年度内容规划工作台。

本阶段不要求总周期规划自动生成具体帖子，也不要求自动生成发布排期。

可以为未来和排期模块连接保留合理的扩展空间，但不需要在本轮实施联动。

# 三、已确认的业务规则

## 1. 规划层级

一条规划主要按照：

```text
学科 + 帖子分类
```

建立。

需要保留学科内部的课程层级或课程结构，以便总周期规划能够表达整个学科下的课程安排。

但不需要增加活动层级，也不需要再增加一层“帖子分类类型”。

请先检查当前仓库中是否已经存在课程相关的数据、配置或页面概念。

如果当前没有正式的课程数据模型，请在 Plan 中提出一个尽量简单、兼容未来扩展的方案，而不是直接假设必须新增复杂课程系统。

## 2. 年度范围

总周期规划固定按照自然年展示：

```text
1 月到 12 月
```

一份规划只属于一个年份，不需要在同一个规划页面中跨年度展示。

跨年度内容可以分别放入两个年度规划。

页面需要能够切换年份，但不需要设计无限时间轴。

## 3. 两类规划表达

当前人工规划表中存在两类不同的数量表达。

### 第一类：弹性规划

例如：

```text
2 × 10
1 × 5
```

以 `2 × 10` 为例，它表示：

* 每个月默认规划 2 篇；
* 持续 10 个月；
* 整个周期最多可以规划 20 篇。

这并不代表每个月必须完成 2 篇。

即使某个月已有帖子的效果很好，不需要制作新帖替代，该月默认的规划名额仍然存在，但实际可以选择做或不做。

因此，这种表达更接近：

```text
默认月度频次 × 持续月份
```

而不是严格的月度硬性任务。

### 第二类：硬性数量组合

例如：

```text
1 + 1
3 + 3 + 4
4 + 4 + 4 + 4
```

这类表达代表当月明确要求发布的帖子数量或帖子组合。

不同数字具体代表什么，未来会由 SOP 或规划模板进一步说明。

当前总周期规划表不需要解释每一个数字具体对应什么，只需要能够准确保存和展示这种组合。

请在数据和交互设计中保留这两类表达之间的区别。

至于底层采用：

* 结构化字段；
* 月度子记录；
* 阶段块；
* 规则类型；
* 结构化数据加展示文本；
* 或其他更合适的方式；

请结合当前项目自行提出方案，不需要被某个预设模型限制。

## 4. 文档和链接

具体内容、规则说明和 SOP 不直接大量写在规划表中。

规划表中需要能够保存并展示：

```text
链接名称 + 链接地址
```

用户点击链接名称后，可以直接跳转到对应网页或文档。

请在 Plan 中评估：

* 一条规划支持一个链接还是多个链接更合理；
* 链接应该属于整条规划还是某个月份；
* 是否需要简单备注；
* 如何复用当前项目已有的 URL 校验能力。

不需要在本阶段建设文档管理系统。

## 5. 版本历史

总周期规划需要完整保留版本历史。

任何调整，无论大小，都应该能够追溯，例如：

* 修改月份；
* 修改数量；
* 修改弹性规划频次；
* 修改硬性数量组合；
* 修改链接；
* 修改备注；
* 修改课程归属；
* 修改显示顺序；
* 启用或停用规划。

请根据当前项目结构提出合适的版本历史方案。

当前项目没有正式的用户、角色和审计系统，因此本阶段不要求顺带建设完整权限平台。

但至少需要保留：

* 历史版本；
* 修改时间；
* 当前版本；
* 历史内容快照；
* 版本之间的差异或可理解的变更信息。

具体实现由你评估。

## 6. 面试课分类

当前面试课没有正式配置帖子分类，允许 `category_id` 为空。

本阶段希望按照已经确定的分类规则，将面试课需要使用的分类补充到现有数据库关系中。

请先检查当前分类字典和种子数据，再在 Plan 中说明：

* 需要补充哪些已有分类关系；
* 是否真的需要新增分类；
* 应通过迁移、种子数据还是其他现有机制处理；
* 如何避免破坏现有面试课数据。

不需要为面试课长期保留一套特殊规划逻辑。

# 四、实现方式由你评估的部分

以下事项暂时没有指定唯一实现，请根据当前仓库情况提出建议：

1. 每个月是否保存独立记录；
2. 是否以阶段块覆盖连续月份；
3. 一个单元格是否同时显示阶段名称、数量和说明；
4. 是否需要单独的规划主表和规划月度明细表；
5. 是否需要将弹性规划和硬性规划拆成不同记录；
6. 链接应该放在哪一层；
7. 课程层级如何最小化实现；
8. 是否需要年度规划复制功能；
9. 如果需要年度复制，应本期实现还是暂缓；
10. 版本历史采用快照、修订表还是其他形式；
11. 年度时间轴应采用表格、甘特式布局还是其他高密度界面；
12. 如何处理同一学科、同一分类在同一年存在多段规划。

请不要为了追求通用性而提前建设过重的配置系统。

优先考虑：

* 当前业务容易理解；
* 当前开发成本可控；
* 数据未来可以扩展；
* 不破坏现有排期管理；
* 页面能够清晰查看和编辑。

# 五、与现有系统的关系

总周期规划应优先复用：

* `operations_subject`
* `operations_category`
* `operations_subject_category`

原则上不要重新建立一套平行的：

* 学科字典；
* 帖子分类字典；
* 学科分类关系。

本阶段通常也不需要改变：

* `operations_content`
* `operations_content_version`
* `operations_content_schedule`

除非你在分析后认为，为未来保持合理关联，需要增加非常轻量且不会影响现有功能的扩展字段。

如果建议修改现有表，请说明：

* 为什么有必要；
* 是否可以通过新增规划表避免；
* 对已有数据和功能有什么影响。

# 六、页面方向

总周期规划是业务页面，应延续现有内容发布控制台的视觉方向：

* 清晰；
* 低动效；
* 高信息密度；
* 中文可读性优先；
* 适合桌面端长期操作；
* 同时考虑移动端的基本查看和编辑能力。

不需要复制总枢纽首页的 3D 穿越效果。

页面大致需要支持：

* 年份切换；
* 按学科查看；
* 按课程层级组织；
* 查看各帖子分类；
* 查看 1 月到 12 月的规划；
* 新建规划；
* 编辑规划；
* 查看链接；
* 查看版本历史。

这只是业务目标，不是固定页面结构。

请结合当前项目已有的 Sidebar、Toolbar、Inspector、Sheet 或其他组件，提出最合适的界面方案。

# 七、本阶段暂不包含

以下功能不属于本次总周期规划表范围：

* 自动生成具体帖子；
* 自动创建内容版本；
* 自动生成发布排期；
* 月例会；
* 周例会；
* 月度应发自动计算；
* 实发、未发和逾期统计；
* 效果补救；
* 跨月自动结转；
* 日粒度复盘；
* 阶段复盘；
* 报表中心；
* 自动报告；
* 正式用户登录；
* 角色和细粒度权限；
* 完整操作者审计；
* 导入导出；
* 生产部署。

如果某些设计会影响这些未来能力，可以在 Plan 中说明，但不要把这些内容纳入本期开发任务。

# 八、请重点检查的现有代码

在输出方案前，请实际查看：

* `src/db/schema.ts`
* `src/db/validation.ts`
* `src/db/taxonomy.ts`
* `drizzle/` 下的迁移文件
* 当前排期页面
* 排期相关 API
* 学科和分类读取逻辑
* 新做帖和编辑排期的 Sheet
* 当前链接校验逻辑
* 当前页面的 Sidebar、Toolbar、Inspector、Sheet
* README 和 docs 中与当前业务结构有关的说明

如果实际文件位置不同，请以仓库为准。

# 九、Plan 需要回答的问题

请输出一份可以供审核的开发 Plan，至少包括以下内容。

## 1. 对当前实现的理解

简要说明：

* 当前相关数据结构；
* 当前学科和分类如何工作；
* 当前可以复用的页面和组件；
* 当前是否已经存在课程概念；
* 当前是否已经存在可复用的版本机制。

尽量注明实际文件位置。

## 2. 推荐的产品结构

说明总周期规划表在系统中的位置，以及建议的页面结构和操作流程。

请说明用户如何：

* 进入某一年；
* 查看各学科；
* 查看课程层级；
* 查看帖子分类；
* 查看 12 个月规划；
* 新建和编辑规划；
* 打开文档链接；
* 查看历史版本。

## 3. 推荐的数据方案

请提出你认为最适合当前项目的数据设计。

需要说明：

* 复用哪些现有表；
* 是否新增规划主表；
* 是否新增月度明细或阶段记录；
* 如何表达弹性规划；
* 如何表达硬性数量组合；
* 如何保存链接；
* 如何保存课程层级；
* 如何保存版本历史；
* 需要哪些约束和索引。

不要求沿用某些固定字段名，按照现有 Drizzle 命名风格设计即可。

## 4. 版本历史方案

请单独说明：

* 什么操作会生成新版本；
* 当前版本和历史版本如何保存；
* 查看历史版本的交互；
* 是否支持恢复旧版本；
* 当前没有正式用户系统时，如何记录基础修改信息。

恢复旧版本是否本期实现，可以由你提出建议。

## 5. 面试课分类补充方案

说明：

* 当前面试课为什么允许空分类；
* 建议补充哪些关系；
* 是否需要新增分类；
* 如何迁移；
* 对已有内容版本有什么影响。

## 6. 页面和交互方案

请说明：

* 推荐使用表格、时间轴还是混合形式；
* 桌面端的主交互；
* 移动端如何降级；
* 编辑使用独立页面、Sheet 还是 Inspector；
* 如何避免 12 个月 × 多学科 × 多分类导致页面过于拥挤；
* 如何显示弹性规划和硬性规划的区别；
* 如何展示链接和版本状态。

可以提供简单的文字线框图。

## 7. API 和服务端方案

列出建议新增的主要接口或服务端操作，包括：

* 获取某年度规划；
* 创建规划；
* 编辑规划；
* 查看历史版本；
* 获取单个历史版本；
* 可能的停用或归档操作。

说明事务、校验和错误处理如何沿用现有项目方式即可，不需要给出完整代码。

## 8. 分阶段实施建议

请将开发拆成若干个可独立验收的阶段。

例如可以包含：

* 数据结构；
* 基础读取和编辑；
* 年度规划页面；
* 版本历史；
* 面试课分类补充；
* 移动端和大数据量验收。

具体如何拆分由你决定。

每一阶段请说明：

* 目标；
* 主要修改范围；
* 验收标准；
* 对现有排期功能是否有影响。

## 9. 待确认事项

请把仍需要业务审核的决定单独列出。

尤其包括：

* 月度记录与阶段块的选择；
* 单元格内容结构；
* 链接数量和归属层级；
* 年度复制是否本期实现；
* 课程层级的具体保存方式；
* 是否允许同一学科和分类存在多条重叠规划；
* 是否支持恢复旧版本。

对于这些问题，请给出你的推荐，而不是只列问题。

# 十、输出要求

* 本轮只输出 Plan，不修改代码。
* 先阅读实际仓库，再作结论。
* 不要假设数据库中存在尚未确认的表。
* 不要重新设计现有帖子、版本和排期系统。
* 不需要机械遵循本文中的示例名称。
* 对未确定的实现，可以大胆提出推荐方案。
* 方案应以最小必要改动为优先，但不必为了少改代码而牺牲数据结构的清晰性。
* 清楚区分“本期建议实施”和“未来可以扩展”。
* 最终 Plan 应足够具体，使我们审核后可以继续让你分阶段实现。

```

这版只固定了真正不能混淆的业务事实，例如年度范围、两种规划含义、版本历史和现有数据库关系。至于用几张表、采用月度记录还是阶段块、页面怎么组织、是否做年度复制，都让 Codex 先根据仓库提出方案。
```
