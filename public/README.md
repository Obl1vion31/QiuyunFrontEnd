# `public/` 静态资源

本目录中的文件会按原路径复制到最终静态站点，不参与 Astro 编译或文件名哈希。

当前主要用于存放首页真实图片；`excel/` 只保留本地导入说明，原始 `.xlsx` 被 Git 忽略，不上传到公开仓库。这里的内容可能被公开访问，因此不得放入密码、Token、员工隐私或未脱敏的敏感业务数据。

图片目录说明见 `images/README.md`，本地导入文件说明见 `excel/README.md`。
